<project name="Minecraft-Multi-Launcher" default="dist">
	<property name="MyApp" value="MCMLauncher" />
	<!-- Create binary distribution -->
	<target name="dist">
		<delete file="${MyApp}.jar"/>
		<jar jarfile="${MyApp}.jar"
         basedir="bin"
         manifest="${MyApp}.mf" />
	</target>
</project>
