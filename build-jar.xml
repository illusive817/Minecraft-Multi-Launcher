<project name="Minecraft-Multi-Launcher" default="dist">
	<property name="MyApp" value="minecraft" />
	<!-- Create binary distribution -->
	<target name="dist">
		<delete file="download/${MyApp}.jar"/>
		<jar jarfile="download/${MyApp}.jar"
         basedir="bin"
         manifest="${MyApp}.mf" />
	</target>
</project>
